# Phase 3 exposure-controlled Push-T rollout

Pre-registered m conditions are compared from an identical saved simulator snapshot for each environment seed. At each replan, an m-independent conditional DDIM clean-trajectory proxy and shared forward noise construct paired reference/self branches. Reported task metrics are only success and episode return.
